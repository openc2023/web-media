# Assets

Use these folders to keep static resources organized:

- `audio/` for recorded clips or reactive samples
- `images/` for UI images or posters
- `models/` for `.glb`, `.gltf`, or other 3D assets
- `textures/` for maps and image-based materials

Current model asset:

- `models/mogu.glb`
